{
  "name": "capacity-tracker",
  "private": true,
  "description": "Work center capacity tracker for SharePoint or any browser.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "bundle": "node scripts/bundle.js",
    "start": "python3 -m http.server 8080"
  }
}
